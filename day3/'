use std::fs::read_to_string;

fn solve(grid: &Vec<&str>) {
    let mut ans = 0;
    
    
    return ans;
}

fn main() {
    let mut grid = Vec::new();
    for line in read_to_string("input").unwrap().lines() {
        grid.push(line.trim());
    }

    solve(&grid);
}
